<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "https://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.example.project.repository.SongRepository">
    <insert id="commentCreate" parameterType="comment">
        insert into comment (user_id, content, date, song_id, artist_id)
        VALUES (#{userId}, #{content}, #{date}, #{songId}, #{artistId})
    </insert>
    <select id="getCommentsBySongId" resultType="comment">
        select * from comment where song_id = #{songId} order by wrote_at desc
    </select>
    <select id="getCommentsByArtistId" resultType="comment">
        select * from comment where artist_id = #{artist_id} order by wrote_at desc
    </select>
    </mapper>